course = {
    university:String,
    name: String,
    description: Array,
    courseCode: String,
    courseType: String,
    expectedCourseDuration: Number,
    courseCompilers: Array,
}

module.exports = course;